Drop background images here, then add a line to `../assets.js`.

Any size works — the app scales it to cover the screen without smoothing, so
pixel art stays sharp. Wide-ish portrait images suit phones best.
