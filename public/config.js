// Fill these in from your Supabase project (Settings → Data API), then commit.
// Both values are safe to publish: the anon key is designed to be public and
// every table is locked down by row-level security to the two emails you add
// to the `members` table in supabase/setup.sql.
//
// Leave them empty to run against the local Node server (`node server.js`).
window.YAP_CONFIG = {
  supabaseUrl: 'https://bzrrmcchtmzturyqnsxs.supabase.co',
  supabaseAnonKey: 'sb_publishable_BCrE3sb7ZjeFzpBwRcVybw_fDrky_Ru',
};
