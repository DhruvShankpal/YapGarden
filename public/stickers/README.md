Drop sticker images here, then add a line to `../assets.js`.

Small squares (64x64 up to 256x256) with a transparent background work best.
They are drawn at roughly 40px on screen, so anything larger is just detail
nobody sees.
